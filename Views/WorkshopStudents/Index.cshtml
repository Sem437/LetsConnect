@model IEnumerable<LetsConnect.Models.WorkshopStudents>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WorkshopId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StudentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkshopId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdStudentWorkshop">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdStudentWorkshop">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdStudentWorkshop">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
